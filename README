RabbitMQ C FreeBSD Port
========================
FreeBSD port files for rabbitmq-c, currently reckon the net/ category is best for these. So at the moment to use them you need to clone and then copy them into `/usr/ports/net`
